Megumiaomiao Bug 修复记录
作者：Aly0（使用 ChatGPT 生成）

说明：从本记录建立后，每次程序修改都会在此追加版本、日期和修复内容。

v0.4.27（2026-08-09）
- 播放器与播放列表改为同一行的左右布局：播放器位于左侧，播放列表位于右侧并与播放器等高。
- 播放列表展开时为播放器预留固定侧栏宽度，不使用覆盖层。
- 收起播放列表后播放器恢复整行宽度，并根据当前视频宽高比自动增加画面高度。
- 窗口缩放和切换横屏、竖屏视频时自动重新计算播放器尺寸。
- 播放列表展开/收起增加 120—170ms 的轻量宽度、尺寸和透明度动画。
- 动画只作用于播放器与侧栏，不重新创建视频卡片，减少动画卡顿。
- 启动时缓存卡片改为每批 32 个渐进渲染，让窗口优先响应。
- 快速扫描改为首屏缓存显示后在后台开始，缩短可操作等待时间。
- 全屏进入和退出会正确保存、隐藏并恢复播放列表状态。


v0.4.26（2026-08-09）
- 播放列表在软件启动时默认收起，只显示紧贴窗口最右边缘的展开按钮。
- 点击视频开始播放时自动展开播放列表；手动收起后可随时从边缘按钮恢复。
- 展开与收起按钮统一为相同尺寸、垂直位置和相反方向箭头。
- 收起状态为按钮预留独立宽度，不再与视频列表滚动条重叠。
- 增加播放列表持久化记忆，加入、移除和清空操作都会立即写入独立 JSON。
- 正常关闭或意外闪退后，下次启动按原顺序恢复仍然存在的媒体。
- 不会因为当前视频库暂时未包含某个播放列表文件而主动清除其持久化路径。


v0.4.25（2026-08-09）
- 播放列表侧栏调整为只与总览视频列表平齐，不再跨越播放器区域或挤压播放画面。
- 播放时收起、展开播放列表不再与进度条、随机播放、音量和全屏按钮重叠。
- 缩短搜索框和排序框宽度，并将动态预览移入设置弹窗，为窗口控制按钮保留固定空间。
- 增加缩略图失败状态持久化；无法处理的媒体失败一次后不再每次启动重复生成。
- 当源媒体文件发生变化时自动清除失败状态并允许重新生成缩略图。
- 为 FFmpeg 媒体任务增加 5 秒单次超时，并减少缩略图回退次数，防止损坏媒体长期卡住扫描和退出。
- 成功生成缩略图后自动清除失败状态。

v0.4.24（2026-08-09）
- 修复多选视频后点击“加入合集”直接加入最后一个合集、没有弹出合集选择列表的问题。
- 批量加入合集现在显示全部合集名称、视频数量和已包含状态。
- 没有合集时提供“新建我的合集并加入所选视频”入口。
- 修复顶部搜索框被窗口拖拽逻辑抢占左键焦点的问题，文本输入控件不再触发拖动窗口。
- 播放列表改为启动后默认常驻右侧，并为播放器和视频库永久预留布局列。
- 播放列表收起后保留恢复标签；切换视频不再覆盖或遮挡侧栏。
- 优化卡片加号菜单的深色外观、间距、勾选状态与多合集层级。

v0.4.23（2026-08-09）
- 修复搜索框左键被顶部窗口拖拽事件拦截，必须右键后搜索才生效的问题。
- 搜索继续使用防抖，并保持输入焦点，不再因结果刷新打断输入。
- 播放列表改为应用启动后默认常驻右侧，不依赖先播放视频或点击播放器按钮。
- 播放列表收起后显示独立恢复按钮，可随时重新展开。
- 修复切换视频时播放器重置边距、重新覆盖右侧播放列表的问题。
- 播放器和视频库始终根据右侧栏状态预留固定空间。
- 优化卡片“＋”弹出菜单为深色层级菜单，播放列表与多个合集选项更清晰。
- 切换视图前主动停止已隐藏卡片的动态预览，降低后台解码、内存占用和关闭延迟。

v0.4.22（2026-08-09）
- 全屏模式下默认仅显示视频画面；鼠标移动到屏幕底部时显示进度条和播放控制，停留结束后自动隐藏。
- 增加单击视频画面暂停/继续，并通过延迟判定避免与双击全屏冲突。
- 精简顶部工具栏，将内嵌播放、缓存目录、mpv 路径和语言切换移入“设置”弹窗。
- 修复搜索输入导致频繁全量刷新问题，增加 280ms 防抖。
- 切换总览、文件夹、合集和搜索时复用已有视频卡片与缩略图，降低界面卡顿和内存抖动。
- 缩短退出时等待 mpv 的超时时间，并继续立即取消 FFmpeg 后台任务。
- 卡片右下角“＋”改为弹出菜单，可选择加入播放列表或指定合集；多个合集以子菜单显示。
- 播放列表改为软件右侧常驻侧栏，支持展开、收起、播放、移除和清空。
- 播放列表展开时自动为播放器和视频列表预留右侧空间。
- 滚动条改为深色圆角窄滚动条。
- MP3 旧缓存中时长为零或封面生成失败的项目会自动重新处理。

v0.4.21（2026-08-09）
- 修复全屏状态下点击“关闭播放器”后窗口仍被全屏层占用、无法点击的问题。
- 全屏改为真正的纯视频画面：隐藏标题栏、播放控制区、列表、边距和圆角，不再调用内嵌 mpv 自身的脱离式全屏。
- Esc、停止播放和关闭播放器都会完整恢复窗口布局。
- MP3 改用专门的内嵌专辑封面提取流程；没有内嵌封面时继续使用同目录名称排序第一张图片。
- 增加文件名实时搜索功能。
- 文件夹浏览增加目录标题及展开/折叠功能。
- 完成合集入口、合集创建、持久化、最新添加视频封面、视频数量、批量加入和合集内容切换。
- 快速扫描会清理合集内已删除视频的失效记录。
- 增加顶部随机播放按钮：优先随机播放播放列表，没有播放列表时随机播放当前合集或视频库。
- 将播放列表入口和随机播放开关从顶部工具栏集成到播放器底部控制区。
- 为新增功能补充英文界面文本。

v0.4.20（2026-08-08）
- 增加 Ctrl + 单击视频卡片多选，Ctrl 操作不会触发播放。
- 增加“总览”和“文件夹浏览”并列视图，文件夹视图按实际目录分组。
- 将 MP3 加入媒体扫描范围。
- MP3 优先提取内嵌封面；没有内嵌封面时使用同目录按名称排序的第一张图片。
- 播放 MP3 时在主播放画面持续显示封面，并保持原始比例。
- 识别同名 LRC 文件，在播放画面下方按时间同步显示较大字号歌词。
- 点击停止按钮后清除封面、歌词和播放状态，并自动收起播放器。
- 建立合集持久化数据模型；合集界面与批量加入入口仍列为下一阶段功能。

v0.4.19（2026-08-08）
- 修复部分短视频因固定在第 2 秒截图而无法生成缩略图的问题。
- 缩略图改为优先截取视频中点，并依次回退到 1 秒、0.1 秒和视频开头。
- 修复部分异常时间戳、轻微损坏或特殊编码视频无法提取画面的问题。
- 为 FFmpeg 增加时间戳重建、损坏帧忽略和首个视频流选择参数。
- 修复动态缩略图在视频中点解码失败后不再重试的问题，失败时回退到视频开头。
- 修复小于 2 秒的视频无法正确生成动态缩略图的问题。
- 修复空白、损坏或生成中断的缩略图缓存被误判为有效的问题。
- 缺少缩略图的视频会在下次快速扫描时自动重新生成。

v0.4.18（2026-08-08）
- 修复缓存只能保存最后一个视频文件夹的问题。
- 每个视频目录改用独立缓存索引，来回切换目录不再重复生成缩略图。
- 每次打开目录快速核对视频清单，只补充新增视频。
- 删除已不存在的视频记录及对应缩略图、动态预览缓存。
- 切换文件夹时取消上一目录的后台生成任务。
- 关闭软件时终止正在运行的 FFmpeg 任务，减少退出卡顿。

v0.4.16（2026-08-07）
- 修复普通双击启动软件后占用 MFP IPC、影响 MFP 识别的问题。
- 普通模式与 MFP 模式改用相互独立的 IPC 管道。

v0.4.14—v0.4.15（2026-08-07）
- 修复收藏、播放列表加号和多选框点击无效的问题。
- 修复视频名称遮挡收藏按钮的问题。
- 调整视频时长和名称位置，提高文字可读性。
v0.4.28（2026-08-09）
- 修复缩略图进度达到“完成数/总数”后仍持续显示、看起来像卡死的问题。
- 缩略图封面增加黑屏检测；若视频中段为黑屏，会自动尝试前后多个时间点。
- 对全程较暗的视频保留最后一个有效画面，避免卡片永久显示为空白或加载中。
- 黑屏检测只在首次生成缩略图时执行，不增加日常浏览负担。
v0.4.29（2026-08-09）
- 修复展开播放列表前后缩略图进度条位置和显示状态不一致的问题。
- 未播放视频时不再显示或允许展开播放列表；开始播放后自动展开。
- 移除播放器控制栏中重复的“播放列表”按钮，统一使用右侧展开/收起入口。
- 收起播放列表后为恢复按钮保留独立窄边，避免按钮被内嵌 mpv 视频画面覆盖。
- 停止播放或关闭播放器时同步收起播放列表及恢复按钮。
- 启动时直接读取已有缓存，不再自动遍历整个视频目录；用户重新选择目录时才执行快速同步。
- 清除意外退出遗留的临时加载状态，已有有效缩略图不会再次显示为生成中。
- 关闭窗口立即隐藏界面，媒体进程和扫描任务在后台快速清理，减少关闭卡顿感。
v0.4.30（2026-08-09）
- 设置中新增“排除小于指定大小的文件（MB）”，在快速扫描阶段过滤。
- 设置中新增“排除短于指定时长的文件（秒）”，读取媒体信息后自动过滤。
- 视频卡片“＋”加入播放列表或合集后，右下角显示 3 秒自动消失提示。
- 扫描器永久排除当前缓存目录和所有 `.VideoShelfCache` 子目录。
- 启动时自动移除媒体库中曾被误识别的缓存预览记录，不删除原始媒体。
- MP3 优先提取内嵌专辑封面作为卡片和播放画面封面。
- MP3 不再生成无意义的动态视频预览，降低处理时间与资源占用。
- 单项媒体失败或被排除不会阻塞后续缩略图任务。
v0.4.31（2026-08-09）
- 修复部分 MP3 标签含未转义控制字符时，ffprobe 完整 JSON 无法解析而导致专辑封面失败的问题。
- 媒体探测只读取时长、宽度和高度，避开异常标题标签并提升加载速度。
- 启动后只补齐缓存中缺失的 MP3 封面和旧版低质量视频封面，不重新扫描目录。
- 视频静态封面从多个时间点候选中选择亮度得分最高的画面。
- 加强近黑画面判断，综合平均亮度与暗像素占比过滤黑屏和接近黑屏画面。
- 旧版封面仅进行一次质量检查，避免真正的暗色视频每次启动都重复生成。
- 缩略图失败原因写入缓存，便于后续准确诊断，不再静默吞掉异常。
v0.4.32（2026-08-09）
- 旧版本中 `ThumbnailFailed=true` 且没有错误详情的记录会自动重试一次。
- 修复源文件未变化时旧失败记录被永久跳过、封面始终不可见的问题。
- 使用 ffmpeg 同时完成媒体信息探测与封面生成，移除重复的 ffprobe.exe。
- 媒体信息改为解析必要的时长和分辨率，不读取容易异常的标签内容。
- 保持免安装、自带运行环境和 MFP/mpv 兼容，不要求用户另装 .NET。
- 发布目录减少约 212MB，同时减少压缩包体积和解压时间。
v0.4.33（2026-08-09）
- 修复双击进入全屏时误触发播放、退出全屏时误暂停的问题；仅单击画面切换播放/暂停。
- 增加 A-B 循环按钮：A 设置起点，B 设置终点，再次点击 A 可清除已启用循环。
- A-B 循环直接使用 mpv 原生循环属性，切换视频时自动清除。
- 全屏状态下鼠标移至底部显示控制栏时，可点击“播放列表”展开或收起右侧列表。
- 非全屏状态不显示全屏播放列表按钮，继续使用右侧边缘展开按钮。
- 新增 WAV、FLAC、M4A、AAC、OGG、OPUS、WMA、APE、ALAC、AIFF/AIF 音频扫描与播放支持。
- 新增音频格式继续支持内嵌封面；无内嵌封面时使用同目录首张图片。
v0.4.34（2026-08-11）
- 将文件夹浏览升级为类似 Windows 文件资源管理器的目录树、面包屑路径、后退和上一级操作。
- 文件夹切换只使用现有媒体缓存，不触发重复缩略图扫描。
- 搜索在当前文件夹及其子目录内生效。
- 合并缓存加载和普通列表的重复渲染流程，减少启动工作量。
- 目录树一次性复用媒体目录索引，降低大量文件夹下的重复计算。
